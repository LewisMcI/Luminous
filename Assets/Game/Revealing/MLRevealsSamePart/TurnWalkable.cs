using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnWalkable : MonoBehaviour
{

    private void Awake()
    {
        TurnVisible turnVis = GetComponent<TurnVisible>();
        if (turnVis != null)
        {
            
        }
    }
}
